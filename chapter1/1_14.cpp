#include <iostream>

int main() {

    // While loops are best for repeating condition/code an unknown
    // number of times, where as for loops are best for repeating 
    // condiion/code a known number of times.

    return 0;
}